import Home from '@arcgis/core/widgets/Home';
import view from '../view.js';

/**
 * Widget that resets location/orientation/zoom settings to their default values.
 */
export default new Home({view});
